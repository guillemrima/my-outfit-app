const db = "" //Añadir la ruta de los modelos cuando esté lista
const User = db.User //Extraer el módelo que nos interesa cuando esté listo


exports.register_user = (req, res)  => {
    console.log("La petición GET ha llegado correctamente al servidor")
}

